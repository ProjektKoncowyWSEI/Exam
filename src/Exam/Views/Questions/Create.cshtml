@model ExamContract.MainDbModels.Question

@{
    ViewData["Title"] = Localizer["Create"];
}

<h1>@Localizer["Create"]</h1>
<partial name="_NicEdit" />
<hr />
<div class="row">
    <div class="col-md-10">
        <form asp-action="Create">
            <input asp-for="ExamId" type="hidden" value="@ViewBag.ExamId" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Content" class="control-label"></label>
                <textarea rows="4" asp-for="Content" class="form-control"></textarea>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AnswerTypesEnum" class="control-label"></label>
                <select asp-for="AnswerTypesEnum" asp-items="Html.GetEnumSelectList<AnswerTypesEnum>()" class="form-control"></select>
                <span asp-validation-for="AnswerTypesEnum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="@Localizer["Create"]" class="btn btn-primary btn-sm" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Exams" asp-action="Index" asp-route-parentId="@ViewBag.ExamId" class="btn btn-primary btn-sm">@Localizer["Back to List"]</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
